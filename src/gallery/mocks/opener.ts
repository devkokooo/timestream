export async function openUrl(url: string): Promise<void> {
  console.info("[specimen] openUrl", url);
}

export async function openPath(path: string): Promise<void> {
  console.info("[specimen] openPath", path);
}
