---
---

<section id="pitch" class="border-t border-tva-gold/16">
  <div class="mx-auto max-w-6xl px-5 py-16 md:px-8 md:py-24">
    <p class="eyebrow mb-4">The graph, not the log</p>
    <h2 class="mb-4 font-display text-[clamp(1.8rem,5vw,3.4rem)] leading-[1.05] font-semibold tracking-[-0.03em]">
      Reconstruct the Sacred Timeline.
    </h2>
    <p
      class="mb-5 font-display text-[clamp(1.05rem,2.4vw,1.6rem)] leading-tight text-tva-gold/45"
      aria-hidden="true"
    >
      Every branch accounted for.
    </p>
    <p class="m-0 max-w-2xl text-sm leading-relaxed text-tva-paper-dim md:text-[0.9375rem]">
      A local-first TVA-styled Git client with GitHub integration.
      <br/><br/>
      Traverse your commit history on the timeline, review diffs, push over SSH,
      then open the pull request. Scroll the path below.
    </p>
  </div>
</section>
